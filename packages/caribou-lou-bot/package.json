{
  "name": "caribou-lou-bot",
  "version": "1.1.0",
  "description": "It's the Microsoft bot that interacts with the user and provides the resume information",
  "main": "./src/index.js",
  "scripts": {
    "build": "docker build -t $npm_package_config_image_name:$npm_package_version .",
    "start": "node ./src/index",
    "start:dev": "nodemon --inspect ./src/index",
    "test": "lab -c"
  },
  "author": "paker",
  "license": "MIT",
  "dependencies": {
    "botbuilder": "^3.14.0",
    "markdown-pdf": "^8.1.1",
    "request": "^2.85.0",
    "request-promise-native": "^1.0.5",
    "restify": "^6.3.4"
  },
  "config": {
    "image": {
      "name": "caribou-lou-bot"
    }
  },
  "devDependencies": {
    "code": "^5.2.0",
    "lab": "^15.3.1"
  }
}
